<resources>

    <!-- Base application theme. -->
    <style name="CleanTheme" parent="android:Theme.Holo.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:buttonStyle" parent="@android:style/Widget.Button">@style/button</item>
    </style>

</resources>
